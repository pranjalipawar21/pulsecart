# PulseCart Frontend — Production Environment Variables
# Used during the production build (npm run build) to point the static client to the hosted Express server.
# Replace with your actual deployed Render, Railway, or Fly.io backend URL.

REACT_APP_API_URL=https://pulsecart-backend-production.onrender.com
